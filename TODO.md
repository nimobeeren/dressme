- use different .env for production deploy (esp client AUDIENCE)

Nice to have:

- set up MinIO for local dev, don't rely on production R2
- somehow pick the right DATABASE_URL depending on running in docker or not
